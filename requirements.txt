# Every dependency pinned to the version installed and tested on 2026-09-12.
# Next step: a hash-pinned lock (pip-compile --generate-hashes) so a build cannot pull a substituted wheel.
Pillow==12.3.0
eth-abi==6.0.0
eth-account==0.13.7
eth-hash[pycryptodome]==0.8.0
eth-utils==6.0.0
fastapi==0.141.1
playwright==1.62.0
requests==2.34.2
uvicorn[standard]==0.52.4
